#include "main.h"
/**
 * _abs - provides absolute value
 * of an integer
 * @n: parameter to be tested if positive
 * or negative
 * Return: 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
